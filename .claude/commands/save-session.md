---
description: Write a verified session snapshot to session_state/SESSION_STATE.md
---

Use the `save-session` skill now.

Follow it as written: verify the real state with the git and docker commands first, then overwrite
`session_state/SESSION_STATE.md` with the snapshot.

This command exists so the skill can be invoked deliberately rather than left to match on its own
description. Do not wait for it to trigger.
