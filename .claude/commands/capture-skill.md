---
description: Invoke the capture-skill skill
---

Use the `capture-skill` skill now, and follow it as written. The skill file at
`.claude/skills/capture-skill/SKILL.md` is the source of truth for what it does; this command only
starts it.

This command exists so the skill can be invoked deliberately rather than left to match on its own
description. Do not wait for it to trigger.
