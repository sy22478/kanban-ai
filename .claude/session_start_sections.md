# Headings in CLAUDE.md to lift verbatim into every session, one per line.
# Matched against any heading that starts with the name, at any depth, so
# "HIPAA rules" finds "## HIPAA rules (non-negotiable)".
# Delete this file to fall back to Security alone.

Security
